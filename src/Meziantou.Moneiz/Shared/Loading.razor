@if (!IsLoading)
{
    @ChildContent
}
else
{
    @: Loading...
}

@code {
    [Parameter]
    public bool IsLoading { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}