name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.300
      
    - name: Exchange rates
      run: curl https://openexchangerates.org/api/latest.json?app_id=${{ secrets.OPENEXCHANGERATES_APIKEY }} --output src/Meziantou.Moneiz/wwwroot/exchangerates.json
      
    - name: Exchange rates (debug)
      run: cat src/Meziantou.Moneiz/wwwroot/exchangerates.json
      
    - name: Test
      run: dotnet test
      
    - name: Publish
      run: dotnet publish src/Meziantou.Moneiz/Meziantou.Moneiz.csproj --configuration Release
    
    - name: GitHub Pages action
      uses: peaceiris/actions-gh-pages@v3.6.1
      with:
        github_token: ${{ secrets.PUBLISH_TOKEN }}
        publish_branch: gh-pages
        publish_dir: src/Meziantou.Moneiz/bin/Release/netstandard2.1/publish/wwwroot
        allow_empty_commit: false
        keep_files: false
        force_orphan: true
        cname: test.moneiz.meziantou.net
